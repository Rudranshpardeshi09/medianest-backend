# The server reads these on Linux; development happens on Windows. A file
# checked out with CRLF and then executed there breaks in ways that are hard
# to see -- a shell line ending in \r fails with "command not found" -- so
# everything textual is normalised to LF in the repository.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
